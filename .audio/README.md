Drop the site's background music here (MP3 preferred; M4A/OGG/OPUS also work).
This folder is git-ignored. Run `pnpm run audio:sync` to upload whatever is
missing from the R2 bucket under `music/`; already-uploaded files are skipped.
